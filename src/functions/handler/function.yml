name: ${self:custom.secrets.service.name}-${self:custom.secrets.provider.stage}-handler
handler: index.handler
memorySize: 128
timeout: 30
reservedConcurrency: 20
events: 
  - http: 
      path: /${self:custom.secrets.service.name}-${self:custom.secrets.provider.stage}-handler
      method: POST
      cors: true
