name: ${self:custom.secrets.service.name}-${self:custom.secrets.provider.stage}-dummy
handler: index.dummy
memorySize: 128
timeout: 30
reservedConcurrency: 20
events: 
  - http: 
      path: /${self:custom.secrets.service.name}-${self:custom.secrets.provider.stage}-dummy
      method: POST
      cors: true
